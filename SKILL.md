---
name: vehicle-speed-precheck
description: 审查视频是否具备后续车辆测速或运动分析所需的时间条件；检查原始文件、PTS/DTS/GOP/packet、重复或冻结画面、OSD时间规律，以及指定车辆逐相邻帧运动连续性。用于视频测速前置审查、跳帧或周期性删帧排查、帧率与时间轴复核、车辆ROI/LK光流分析和可追溯技术报告生成。不计算实际物理车速，不进行道路尺度、透视或单应性标定。
---

# 视频测速前置审查

## 任务边界

判断视频的编码时间、画面内容更新、OSD时间和目标运动是否足以支持后续测速或运动分析。不得把像素位移、像素速度或检测框速度写成实际车速；不得根据单项指标认定删帧、插帧、冻结或具体处理软件。

## 输入

至少取得原始视频路径和待检时间或帧号区间。尽量取得：

- 视频来源、文件状态和是否为原始导出；
- OSD完整区域和秒字段区域；
- 指定车辆、候选车辆、ROI或锚点框；
- 已知遮挡、制动、加速、转向、碰撞或机位移动情况。

缺失信息时继续完成可验证部分，并把其余维度标记为“无法评价”；不得虚构ROI、OSD文字或运动状态。

## 文件加载

- 所有任务先完整读取 `references/pipeline.md`。
- 选择测量方法时读取 `references/method-selection.md`。
- 执行逐相邻帧车辆分析时完整读取 `references/motion_continuity_sop.md`。
- 涉及刚体、透视、多特征点或物理运动解释时读取 `references/marked_vehicle_consistency.md`。
- 解释异常及比较替代假设时读取 `references/evidence-and-failure-modes.md`。
- 生成CSV、JSON或交接数据时读取 `references/data-contract.md`。
- 生成最终报告前读取 `templates/report-outline.md`。

不要无条件加载所有参考文件。

## 核心流程

1. 固定原始证据：记录文件哈希、大小、路径、工具版本和运行参数；直接读取原文件，不转码、不补帧、不改变帧率。
2. 建立编码时间证据：保存逐帧PTS/DTS、帧型、packet和GOP信息；统一使用 `rel_pts_time = pts_time - first_pts_time`。
3. 检查画面内容更新：检查重复/近重复、冻结、差分突变和解码错误。
4. 检查OSD：识别逐帧OSD值，定位跳秒帧，区分完整秒与边界秒。
5. 选择目标和方法：根据画面条件选择LK光流、检测框、轮廓中心、稠密光流或人工测点；方法可以变化，数据和证据规则不得变化。
6. 执行全部严格相邻帧对测量；保存原始值、质量值和不可评价状态。
7. 先产生异常候选，再比较时间异常、跟踪失败、遮挡、透视、物理运动变化和画面突变等假设。
8. 输出结构化数据、图表、证据图片和边界清晰的报告。

## 运动连续性硬规则

1. 覆盖用户区间内全部 `frame N → frame N+1`；报告唯一帧对数、覆盖率和不可评价帧对。
2. 在完整覆盖方案中使用最少车辆；车辆片段由实际可评价范围决定，不按时间区间互斥切割。
3. 条件允许时在交接处保留2至3个重叠帧对。重叠车辆分别测量，不混合特征点。
4. 每辆车独立建立局部正常投影位移和倍率基线。
5. 不跨车辆连接原始 `dx`、`dy` 或像素位移；跨车辆只比较格式化倍率。
6. 重叠帧需要单行汇总时，用重叠车辆倍率的稳健共识；`dx/dy`仍注明其代表车辆。
7. 遮挡、出画、混入其他目标、有效点不足、误差超限或物理运动突变时终止当前车辆片段。
8. 不可评价帧对不得插值；平滑曲线不得覆盖原始相邻帧测量。
9. FFT和自相关只能描述已定位候选的节律，不能单独产生候选或原因结论。
10. 异常标签使用变化后的到达帧号。

## 倍率与OSD推定计数

定义：

```text
motion_ratio = projected_displacement / local_expected_displacement
```

比例必须来自沿车辆主运动方向的投影位移，不得仅用单独的 `dx` 或 `dy`。阈值是按视频校准的经验参数，不是行业标准。

需要验证内容采样模型时，可按本案配置把倍率换算为推定原始帧间隔增量。例如：

```text
ratio < 1.6       → +1
1.6 ≤ ratio < 2.6 → +2
2.6 ≤ ratio < 3.6 → +3
后续依次类推
```

在每个完整OSD秒内求和。把结果称为“推定原始帧间隔数”，不得称为直接读取到的原始帧数；边界秒只展示，不参与模型判断。

## 帧图片编号与命名

1. 以原视频实际可解码首帧为第1帧，按全片解码显示顺序确定`图片帧序号`；不得用`round(PTS × 帧率) + 1`替代实际解码序号。
2. 截取局部区间时保留全片`图片帧序号`，不得从截取区间首帧重新编号；非连续选帧时保留原序号间隔。
3. 帧图统一命名为`帧*N.png`。令`width = len(str(max(本次全部图片帧序号)))`，再将每个帧序号用前导零补齐到`width`位。不得固定宽度，也不得按截取顺序重排编号。例如本次导出帧序号为`7、42、105`时，`width = 3`，依次命名为`帧007.png`、`帧042.png`、`帧105.png`。
4. 逐帧CSV使用`图片帧序号`与`图片文件`建立一一映射。默认不另设“导出序号”或“源帧序号”；用户明确要求时才增加。
5. `OSD秒内帧序号`是同一显示OSD秒内的计数，必须独立保存，不得用于帧图命名或替代`图片帧序号`。

## 输出

至少保存：

- 原始文件信息、哈希、工具版本和 `info.txt`；
- 指定区间全部原始解码帧及`图片帧序号`/PTS映射；帧图沿用全片解码序号并按统一动态位宽的`帧*N.png`命名；
- 全部车辆片段逐帧测量CSV，包括重叠测量；
- 一行一个唯一到达帧的汇总CSV，至少包含：帧序号、OSD时间、成品OSD秒间帧间隔数、倍率推定原始帧间隔数、`dx_px`、`dy_px`、代表车辆倍率、运动不连续共识倍率、推定原始帧间隔增量、采用车辆、重叠车辆数和质量字段；
- 候选CSV、OSD秒统计、车辆片段表和不可评价帧对；
- 分车辆原始曲线、交接处断线的格式化倍率曲线、候选标注图；
- HTML报告和文字稿。

另外输出一份面向人工复核的精简CSV，只保留：`帧序号`、`PTS时间_s`、`OSD时间`、`OSD秒间帧数`、`dx_px`、`dy_px`、`运动不连续共识倍率`、`异常标记`。其中 `OSD秒间帧数` 不是成品帧行号或秒级总数，而是同一显示OSD秒内按 `inferred_step` 逐行累加的倍率推定计数；OSD秒值变化时清零后重新累计。`异常标记` 在共识倍率达到本案候选阈值且质量合格时写入“异常”，其余留空。

精简表不得替代完整审计数据。代表车辆、局部基线、倍率来源、重叠结果和质量字段保存在完整汇总表与逐车辆测量表中。

交付目录使用专业、中性且按审阅顺序编号的名称，并提供交付索引。推荐顺序为：技术报告、分析数据、结果图表、审查证据、复现资料；不得把所有产物堆放在根目录。

所有图表和报告必须来自同一份结构化数据。

汇总行使用跨车共识倍率时，必须同时保存代表车辆倍率和共识倍率来源；不得让代表车辆的位移/基线与共识倍率看似可直接相除。候选阈值、图中阈值线、代码配置和报告文字必须一致。

## 结论口径

分别陈述：编码时间轴、画面内容更新、OSD规律、目标运动连续性和综合推断。优先使用“支持……假设”“与……现象相符”“不能排除……”和“现有材料不足以认定……”。缺少原始码流或生成链记录时，不认定具体软件、处理环节或法律意义上的删帧事实。
