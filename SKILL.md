---
name: video-metadata
description: 提取视频元数据和指定区间帧图片，并从PTS时间轴、指定车辆逐相邻帧运动连续性、OSD时间码三个独立维度复核视频时间采样；定位运动不连续的具体到达帧，验证原始采样帧率与成品编码帧率是否可能不一致。用于视频测速预检、帧率/时间轴复核、周期性删帧或插帧排查、车辆速度矢量分析和SOP报告生成。
---

# 视频测速预检与三维度验证

## 核心流程

按顺序执行：

1. 用 `extract.ps1 -Simplified` 提取 `info.txt` 和指定区间全部原始帧。
2. 分别完成三个独立维度：
   - 维度一：PTS编码时间轴；
   - 维度二：指定车辆逐相邻帧运动连续性；
   - 维度三：OSD时间码及每秒实际帧数。
3. 综合解释三个维度的相同或不同结果。
4. 输出HTML报告、文字稿、原始帧、逐帧测量CSV、候选帧CSV、格式化曲线和候选帧标注。

三个维度不要求彼此一致。维度二发现的画面采样不连续，可以在PTS和OSD均连续时独立成立。

## 第一步：提取

```powershell
$skillDir = "<本技能目录的绝对路径>"
& (Join-Path $skillDir "extract.ps1") `
  -VideoPath "<视频绝对路径>" `
  -StartTime 340 `
  -Duration 7 `
  -Simplified
```

时间统一使用：

```text
rel_pts_time = pts_time - first_pts_time
```

简化模式最终可只展示 `info.txt` 和帧图片，但维度二分析期间必须保留逐帧原始测量数据；不得只保留平滑曲线或FFT结果。

## 维度一：PTS

从 `info.txt` 检查：

- `first_pts_time`；
- 相邻帧 `delta_pts_time`；
- 重复PTS、双倍间隔、跳变和缺帧；
- GOP、帧类型和packet连续性。

PTS均匀只能证明成品编码层时间戳连续，不能证明画面内容按相同间隔采样。

## 维度二：指定车辆逐相邻帧运动连续性

必须读取 `references/motion_continuity_sop.md`。需要解释刚体、透视、多个车身特征点或物理运动状态时，再读取 `references/marked_vehicle_consistency.md`；若旧参考与本SOP冲突，以本SOP为准。

核心硬规则：

1. 必须覆盖用户选取区间内全部相邻帧对；报告覆盖帧对总数和覆盖率。
2. 在完整覆盖方案中选取车辆数量最少：
   - 1辆能覆盖时不得选2辆；
   - 2辆能覆盖时不得选3辆。
3. 车辆数量相同时，优先选像素面积更大、纹理更清楚、遮挡更少、跟踪质量更高的组合。
4. 每一车辆片段只分析一辆身份明确的车辆；不得混合多车特征点。
5. 每辆车独立建立局部正常位移和候选阈值。
6. 只计算严格相邻帧 `frame N → frame N+1`；跨帧轨迹不得当作单帧位移。
7. 遮挡、出画、目标框混入其他车辆、有效特征点不足或反向误差超限时，终止当前车辆并寻找下一辆。
8. 车辆交接帧共享；不得漏掉交接处相邻帧对。无法覆盖时明确标记“不可评价”，不得插值。
9. FFT不是必做项，也不能单独认定运动不连续。

基础测量：

```text
dx = x_i - x_(i-1)
dy = y_i - y_(i-1)
displacement = sqrt(dx² + dy²)
```

每个帧对使用合格车身特征点沿该车主运动方向投影位移的中位数。使用LK正向光流和反向回验；推荐起始质量门槛：

- 目标框面积约不低于1000 px²；
- 短边不低于20 px；
- 有效特征点不少于15个；
- 前向—反向中位误差不高于0.8 px。

面积只作初筛；遮挡和跟踪质量不合格时仍不得使用。

### 格式化曲线

不同车辆的原始像素位移不得直接连接。分别计算：

```text
格式化位移 = 当前相邻帧位移 / 该车辆局部正常位移
```

再按全局到达帧号绘制综合曲线：

- 正常基线标为 `1.0`；
- 车辆交接处断线并标注片段编号；
- 候选点标红；
- 异常标签只写变化后的到达帧号，例如 `07183→07184` 标注 `7184`；
- 完整帧对保留在明细表和CSV。

## 维度三：OSD

- 识别每帧OSD秒值；
- 统计每个完整OSD秒实际包含的帧间隔数；
- 末端不足一秒时标记“边界”，不作为异常；
- 生成OSD秒间帧数分布图。

## 帧率模型验证

先由维度二定位异常帧，再结合OSD每秒帧数验证模型。例如：

```text
成品每秒21个帧间隔
每秒4次双帧跨度
17×1 + 4×2 = 25个推定原始帧间隔
21÷4 = 5.25个成品帧间隔/次
```

若每个完整OSD秒均出现约4次较大位移，候选主要间隔5或6帧，且较大位移接近局部正常位移的2倍，可表述为“支持25 fps内容周期性删除约4帧/秒后形成21 fps成品视频的假设”。缺少原始码流和生成链记录时，不认定具体软件或处理环节。

## 报告要求

报告至少包含：

1. 视频、PTS和OSD基本信息；
2. 选车组合及为何是满足完整覆盖的最少车辆方案；
3. 每辆车的实际帧号范围、目标描述、面积/特征点/误差和终止原因；
4. 全区间覆盖帧对数、覆盖率及不可评价帧对；
5. 分车辆原始位移曲线；
6. 综合格式化曲线；
7. 候选到达帧号及完整帧对明细；
8. 每个OSD秒的候选次数和推定原始帧间隔；
9. 结论强度与边界说明。

像素位移不是实际物理车速。实际测速仍需尺度、透视或道路平面标定。
