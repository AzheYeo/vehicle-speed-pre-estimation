---
name: video-metadata
description: 对固定机位、带秒级画面 OSD 的监控视频进行“可测速性”检验，判断原生时间轴、OSD 跳秒规律和画面运动是否连续，并以 A/B/C/D/U 候选等级和人工签核状态交付自包含 HTML 技术报告；也用于视频取证中的文件哈希、FFprobe 元数据、逐帧 PTS/DTS、帧哈希、包表、指定时间或帧号附近图片提取。用户提到 video-data、video-metadata、视频是否可测速、跳帧、冻结、OSD 重建或运动连续性时使用。只判断后续测速所需的时间条件，不计算真实车速，不进行道路尺度或透视标定。
---

# 视频可测速性判定

## 任务边界

判断视频是否具备后续测速所需的时间条件，不计算车辆真实速度。

- 第一版仅适用于固定机位、画面烧录秒级 OSD 的监控视频。
- OSD 只能验证相对时间规律，不能自动证明绝对日期真实。
- 不把扩展名、声明帧率或单一自动指标作为否决依据。
- 不先转码、补帧、插帧、剪辑或改变帧率；始终直接读取原文件。
- 自动输出只能是等级候选；完成人工签核后才能形成正式等级。
- 本技能仅包含工作流文字，不依赖技能目录中的专用脚本。

## 需要的输入

至少取得原视频路径。尽量取得以下信息；缺失时先做可完成的检查，并在报告中标为待确认：

- OSD 完整文字区域和秒字段区域；
- 需要排除的 OSD、水印或隐私区域；
- 用于运动见证的道路区域；
- 指定时间点、帧号或需要提取图片的区间；
- 人工确认的首个 OSD 时间；
- 检验人员、来源说明和人工复核状态。

用户未明确 OSD 或运动区域时，先提取代表帧供其标注，不要凭想象固定 ROI。

## 工作流

### 1. 固定原始证据

记录原文件的：

- SHA-256；
- 文件名、字节数、修改时间和完整路径；
- FFmpeg、FFprobe、OpenCV、OCR 工具及运行环境版本；
- 完整 FFprobe JSON 和解码错误输出；
- 实际封装、编码、分辨率、时基、`r_frame_rate`、`avg_frame_rate`、时长和流数量。

文件扩展名、实际封装、文件名日期、元数据日期或 OSD 日期不一致时，列为来源风险，不自动否定相对时间连续性。

### 2. 保留元数据、帧表和图片提取能力

按显示顺序逐帧编号，从 0 开始。逐帧证据至少记录：

- 帧序号；
- PTS、DTS、best-effort timestamp 和相对 PTS；
- 相邻 PTS 间隔、帧时长和帧类型；
- 包位置、包大小和关键帧状态；
- 帧哈希、重复状态及运动质量状态。

相对时间必须以首个有效 PTS 为零点：`relative_pts = pts - first_pts`。不得把容器绝对 PTS 直接写成视频相对时间。

需要更底层复核时，可保留包表及 Program Stream/PES 层时间戳结果，但只能作为 FFprobe 的交叉证据。

当用户要求指定时间点或帧号附近图片时：

- 默认提取目标点前后各 2 秒；用户指定窗口时服从用户范围；
- 只导出视频中真实存在的帧，不补齐缺失帧；
- 使用 `frame_<视频流帧号6位补零>.png` 命名；
- 保留图片与帧序号、PTS、相对时间的映射；
- 计算导出图片哈希；
- 对跳变点同时展示前一帧、异常帧和后一帧；
- 对 OSD 跳秒锚点保存包含完整 OSD 和秒字段的证据图。

### 3. 建立三套时间证据

分别检查：

1. 帧序号：实际解码顺序和帧数量；
2. PTS/DTS：非递增、重复时间戳、长间隔、时基变化和分段；
3. OSD：格式、单调性、跳秒位置、OCR 置信度和长期斜率。

OCR 只识别跳秒后的稳定帧组。低置信、缺字、时间倒退或异常跳秒必须标记为人工确认，不能自动修正后隐藏原结果。

把相邻两个 OSD 跳秒锚点的帧号差称为“OSD 跳秒锚点间帧数”。该值可能包含字符切换、压缩模糊和边界检测漂移，不得直接解释为某一自然秒的真实输出帧数或丢帧数。

### 4. 检查画面运动连续性

屏蔽 OSD 后依次检查：

- 完全重复帧、近似重复段和冻结；
- 背景特征估计的机位抖动或移动；
- 画面差分突跳；
- 经过质量门控的稀疏 Lucas-Kanade 光流；
- 近、中、远三个代表性运动目标的人工见证。

LK 只使用清晰、持续至少三帧、通过前后向误差检查并完成背景仿射补偿的轨迹。特征或运动信息不足时标为 `LOW_SIGNAL`：既不计为 PASS，也不自动判为异常。

“速度增大 2 倍”只生成候选。正式候选还应满足多轨迹同步增大、覆盖多个空间区、方向变化可解释，并在后续帧出现恢复形态。车辆急刹、碰撞、转弯、遮挡或新目标进入画面必须人工排除。

全局 LK 只能用于初筛。正式案件必须人工选取近、中、远至少三个清晰运动目标，每段不少于 1 秒；无有效运动目标时结论为 U。

### 5. 必要时用 OSD 受限重建时间轴

以跳秒锚点 `(frame_index, osd_second)` 做稳健回归：

`estimated_time(i) = a + b × i`

- 不在每个 OSD 秒内强制均匀插值；
- 不生成不存在的帧；
- 变帧率或明确时间跳变时分段拟合；
- 禁止跨分段边界使用；
- 所有重建时间必须标为估计值。

OSD 单独重建至少需要 10 个连续有效锚点、跨度不少于 10 秒。默认要求：

- 与 PTS 的长期斜率差不超过 0.5%；
- 锚点残差绝对值 P95 不超过 0.25 秒；
- 不存在超过 0.5 秒的未解释残差阶跃；
- OSD 单调且跳秒关系可解释。

时间不确定度：`u = max(一帧时长, OSD 回归残差绝对值 P95)`。

默认时间误差贡献上限为 5%，则最短建议窗口：`ceil(2u / 0.05)` 秒。

### 6. 形成候选等级

- A：PTS 严格连续、运动连续、OSD 与 PTS 长期斜率一致。
- B：PTS 不可靠，但 OSD 重建资格、运动连续性和最短窗口要求全部满足。
- C：异常边界已定位，异常前后分别完成时间轴与运动验证；禁止跨段使用。
- D：存在无法解释或无法定位的冻结、乱序、缺失，或三类证据相互冲突。
- U：OSD 锚点或有效运动证据不足，且不满足其他明确等级。

PTS 清零、重复或长间隔本身不自动排除 B。C 必须有保存下来的分段边界和各段拟合结果，不能仅凭“发现局部异常”给出。

## 人工签核

自动候选等级只有在以下四项全部完成后才能转为正式等级：

- OSD 内容、跳秒位置和低置信 OCR 已核对；
- 近、中、远运动见证已核对；
- 重复、冻结、画面突跳和 LK 候选已核对；
- 文件日期、OSD 日期、封装和声明帧率等来源风险已确认知悉。

同时记录复核人员、复核时间和备注。任一项未完成时，HTML 首页必须显示“候选 / 待人工签核”，正式等级留空。

## HTML 最终交付

默认只向用户交付一个自包含 HTML 技术报告作为主要成果；底层 CSV、JSON 和证据图片可保留作审计附件，但不得替代 HTML。

HTML 按以下阅读顺序组织：

1. 标题、案件范围和自动候选/正式状态；
2. 技术摘要与结论边界；
3. PTS、OSD 跳秒锚点间帧数、LK 质量门控三类可视化；
4. 指标定义和方法；
5. LOW_SIGNAL、OSD 锚点、异常区间和来源风险明细；
6. 不确定度、最短窗口和不能据此推出的结论；
7. 四项人工签核状态与下一步。

报告必须：

- 使用中文，结论先行；
- 明确“可测速”只表示时间条件可用；
- 图表与叙述使用同一份结构化数据；
- 每个图表写清单位、样本数、定义和解释限制；
- LOW_SIGNAL 使用独立状态，不用绿色“通过”表达；
- 将 OSD 指标写作“跳秒锚点间帧数”，不写成“实际每秒帧数”；
- 嵌入必要的样本帧或使用可随 HTML 一起交付的安全相对资源；
- 不依赖远程脚本、CDN 或联网资源；
- 不在页面中暴露机器私有绝对路径；
- 保留文件哈希、工具版本、配置摘要和证据来源；
- 在人工签核前醒目显示“候选 / 待人工签核”。

## 最终回答口径

向用户提供 HTML 文件链接，并简要说明：候选等级、正式签核状态、关键时间证据、LOW_SIGNAL 数量、异常区间和主要来源风险。不要在最终回答中声称已经完成真实测速。
