# 运动连续性触发用例

## 应触发 `video-motion-continuity`

1. `使用已有基础CSV和帧图片复核帧103到帧118的运动连续性。`
2. `检查这个区间是否存在疑似跳帧，并把异常写在到达帧。`
3. `分析指定车辆在相邻帧中的运动倍率并修正OSD秒间帧序号。`

预期：保留基础CSV，生成独立的运动分析CSV；覆盖指定区间全部严格相邻帧对。

## 边界用例

1. 缺少基础CSV或对应帧图时，只索取必要材料，不虚构测量结果。
2. 目标遮挡、出画或质量不足时标记不可评价，不插值。
3. 用户要求实际车速时，说明当前结果仅为像素运动或无量纲倍率。
