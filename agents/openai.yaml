interface:
  display_name: "视频测速预检与运动连续性验证"
  short_description: "逐车覆盖待检区间，定位运动不连续帧并复核时间采样"
  default_prompt: "Use $video-metadata to extract the selected video interval, verify PTS and OSD independently, and perform minimum-vehicle full-coverage adjacent-frame motion continuity analysis with labeled anomaly arrival frames."
